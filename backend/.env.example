# Solo para correr la API fuera de Docker (npm start). En Docker usa el .env de la raíz.
PGHOST=localhost
PGPORT=5432
PGDATABASE=productos_icr
PGUSER=postgres
PGPASSWORD=cambia_esto
PORT=4000
# Usuario/clave del panel /admin (si ADMIN_PASSWORD está vacío, el panel queda abierto)
ADMIN_USER=admin
ADMIN_PASSWORD=
